# Usage

Run the pane from any Git repository:

```sh
python3 /path/to/git_branch_pane.py . --port 8765
```

For an SSH host, run the pane on the remote host and tunnel the port back to your local machine.
