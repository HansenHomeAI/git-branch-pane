#!/usr/bin/env sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)

if [ "$#" -eq 0 ]; then
  set -- .
fi

EXTRA_OPEN=
if [ "${GBP_OPEN:-1}" != "0" ] && [ -z "${SSH_CONNECTION:-}" ]; then
  EXTRA_OPEN=--open
fi

exec python3 "$SCRIPT_DIR/git_branch_pane.py" "$@" $EXTRA_OPEN
